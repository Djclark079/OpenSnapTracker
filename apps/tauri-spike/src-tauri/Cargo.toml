[package]
name = "opensnaptracker-tauri-spike"
version = "0.1.0"
edition = "2024"
license = "MIT"

[workspace]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lints.rust]
unsafe_code = "forbid"
